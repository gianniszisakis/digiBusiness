document.addEventListener('DOMContentLoaded', () => {
  "use strict";

  /**
   * Show the GR/GB menu and hide the GR/GB menu
  
  const grMenuBtn = document.getElementById('grMenu');
  const gbMenuBtn = document.getElementById('gbMenu');
  const grMenuDiv = document.getElementById('menuGR');
  const gbMenuDiv = document.getElementById('menuGB');

  // Add click event listener to the greek menu
  grMenuBtn.addEventListener('click', function(){
    
    gbMenuDiv.style.display = 'none';
    grMenuDiv.style.display = 'flex';
  })

  // Add click event listener to the gb menu
  gbMenuBtn.addEventListener('click', function(){
    
    grMenuDiv.style.display = 'none';
    gbMenuDiv.style.display = 'flex';
  })
   */

  /**
   * Preloader
   */
  const preloader = document.querySelector('#preloader');
  if (preloader) {
    window.addEventListener('load', () => {
      preloader.remove();
    });
  }
  /**
   * Initiate glightbox
   */
  const glightbox = GLightbox({
    selector: '.glightbox'
  });

  /**
   * Initiate pURE cOUNTER
   */
  new PureCounter();

  /**
   * Init swiper slider with 1 slide at once in desktop view
   */
  new Swiper('.slides-1', {
    speed: 600,
    loop: true,
    autoplay: {
      delay: 5000,
      disableOnInteraction: false
    },
    slidesPerView: 'auto',
    pagination: {
      el: '.swiper-pagination',
      type: 'bullets',
      clickable: true
    },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    }
  });

  /**
   * Init swiper slider with 3 slides at once in desktop view
   */
  new Swiper('.slides-3', {
    speed: 600,
    loop: true,
    autoplay: {
      delay: 5000,
      disableOnInteraction: false
    },
    slidesPerView: 'auto',
    pagination: {
      el: '.swiper-pagination',
      type: 'bullets',
      clickable: true
    },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
    breakpoints: {
      320: {
        slidesPerView: 1,
        spaceBetween: 40
      },

      1200: {
        slidesPerView: 3,
      }
    }
  });

 

  /**
   * Animation on scroll function and init
   */
  function aos_init() {
    AOS.init({
      duration: 1000,
      easing: 'ease-in-out',
      once: true,
      mirror: false
    });
  }
  window.addEventListener('load', () => {
    aos_init();
  });



});